import  {nothing}  from '../client/js/submitForm'
describe('Test, the function "nothing()" should exist' , () => {
    test('It should return true', async () => {
        expect(nothing).toBeDefined();
    });
});
